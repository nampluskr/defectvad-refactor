_base: ../_base.yaml

meta:
  task_name: anomaly

data:
  name: visa_anomaly
  root: ${paths.dataset_root}/visa
  params:
    category: candle
  image_size: [256, 256]
  batch_size: 8
  num_workers: 4
  drop_last: false
  split:
    mode: file
    path: configs/anomaly/splits/visa_candle.json
  transform:
    train:
      name: anomaly_default
      params: {}
    eval:
      name: anomaly_default
      params: {}

selectors:
  category:
    data.params.category: "{value}"
    data.split.path: "configs/anomaly/splits/visa_{value}.json"
